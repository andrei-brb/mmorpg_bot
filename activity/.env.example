# Same value as "Application ID" in Discord Developer Portal → General Information
# (OAuth2 client id — not the bot token.)
VITE_DISCORD_CLIENT_ID=

# Leave empty when the Activity and /api are on the same origin (recommended).
# If the UI is on Vercel and the API on Railway, set this to the API base, e.g. https://your-bot.up.railway.app
# VITE_API_BASE_URL=

# Local only: where Vite proxies /api (default http://127.0.0.1:8080 — your bot HTTP port).
# VITE_DEV_PROXY_TARGET=http://127.0.0.1:8080
